apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ui-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/GoormPlay/ui-service.git
    targetRevision: deploy
    path: k8s/helm
    helm:
      valueFiles:
        - values.yaml
    submodules: true
  destination:
    server: https://kubernetes.default.svc
    namespace: ui
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
